//Esta libreria nos permitira manejar datos falsos como si fuese una base de datos real
/*import 'package:flutter/widgets.dart';
//renombramos al paquete para que cuando usemos sus funciones, no se confundan
import 'package:flutter_modulo1_fake_backend/modulo1_fake_backend.dart'
    as server;

import 'package:flutter_modulo1_fake_backend/models.dart';

class ServerController {
  //Este metodo inicializa datos en el fake_backend, esto no se hace cuando nos comunicamos con un servidor
  void init(BuildContext context) {
    //generateDate: genera todos los datos
    server.generateData(context);
  }

  Future<User> login(String userName, String password) async {
    return await server.backendLogin(userName, password);
  }
}*/
